# Maven 配置
maven:
  # 指定 Maven 版本
  version: 3.6.3
  # 配置 Maven 快照版本的命名方式
  snapshot:
    # 使用时间戳作为快照版本的后缀
    timestamp: 'yyyyMMddHHmmss'
    # 是否包含 Git 提交 ID
    withGitBranch: false
    withGitSha1: false