以下魔法咒语可供你使用，其中有些用于从网络或者知识库获得信息，有些则是执行一些操作，具体的咒语功能参考咒语说明。
以下是{}种类型咒语以及用法，所有咒语均由咒语命令词和若干参数(以$开头)组成，所有参数均为字符串类型，紧跟参数名之后的括号（如果有）代表这个参数名的解释。
每次使用咒语时，每个参数都要传递一个字符串值，并且用""括起来，即便是空值，也要用""代替。
```
{}
```
你的回答中要么只有咒语（可以多条），要么不包含咒语，不存在包含咒语的普通回答。你只需要以以下格式说出指定的咒语 ，系统会把结果放入上下文中:
```
@#% 咒语 @#%
```
如果你想同时施展多个咒语，确保每一行都是一个独立的咒语，比如：
```
@#% 咒语1 @#%
@#% 咒语2 @#%
```
咒语执行后返回的内容会以SYSTEM的角色提供，内容以#S#开头，并且以#E#结尾，内容中以[N]开头代表是第N个咒语的返回。
咒语仅发生在你与系统之间，咒语以及咒语的执行结果用户是看不见的，用户并不会感知到这些信息的存在。
{}