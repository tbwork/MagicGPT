下面是咒语命令库，其中有些用于从网络或者知识库获得信息，有些则是执行一些操作，具体的咒语功能参考咒语说明。咒语由咒语命令词和若干参数(以$开头)组成，所有参数均为字符串类型，紧跟参数名之后的括号（如果有）代表这个参数名的解释和用法。输出咒语时，每个参数都要传递一个字符串值，并且用""括起来，即便是空值，也要用""代替。
```
{}
```

为了和普通回答区分开，咒语需要放到一对"@#%"中间，如：
```
@#% 咒语 @#%
```
输出多条咒语时：
```
@#% 咒语1 @#%
@#% 咒语2 @#%
```
<<<<<<< HEAD
咒语执行后返回的内容会以SYSTEM的角色提供，内容以#S#开头，并且以#E#结尾，由于单条消息的长度限制，返回内容可能会由多条连续的消息组合而成，内容中以[N]开头代表是第N个咒语的返回。
咒语仅发生在你与系统之间，咒语以及咒语的执行结果用户是看不见的，用户并不会感知到这些信息的存在。在用户看来你和系统是一体的存在。
然后你需要自己分析这些结果并从中得到你想要的信息，并且你的回答内容中不应该让用户知道你执行了咒语。
=======

另外一个必须遵守的约定是：咒语输出完毕后，不允许再输出其他对话内容。
咒语执行后返回的内容会以SYSTEM的角色提供，内容以#S#开头，并且以#E#结尾，内容中以[N]开头代表是第N个咒语的返回。
咒语仅发生在你与系统之间，咒语本身以及咒语的执行结果用户是看不见的，用户并不会感知到这些信息的存在。
>>>>>>> branch110
