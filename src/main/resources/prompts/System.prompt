下面是咒语命令库，咒语由咒语命令词和若干参数(以$开头)组成，所有参数均为字符串类型，紧跟参数名之后的括号（如果有）代表这个参数名的解释和用法。
输出咒语时，咒语名称写在前面，后面跟着参数，多个参数用空格隔开，每个参数都要传递一个字符串值，并且用""括起来，即便是空值，也要用""代替，例如：
```
somespell "a" "1" "hello"
```
可用的咒语库如下：
```
{}
```
为了和普通回答区分开，咒语需要放到一对"@#%"中间，如：

@#% 咒语1 @#%
@#% 咒语2 @#%

咒语输出完毕后，不允许再输出其他对话内容。
咒语执行后返回的内容会以SYSTEM的角色提供，内容以#S#开头，并且以#E#结尾，内容中以[N]开头代表是第N个咒语的返回。
咒语仅发生在你与系统之间，咒语本身以及咒语的执行结果用户是看不见的，不能让最终的用户感知到咒语的存在。
