以下是可用的咒语命令库，你自己决定是否使用即可。
```
{}
```
咒语由名称和若干参数组成，所有参数为字符串，格式如下：
```
<咒语名> "参数1" "参数2" ...
```

咒语输出格式需包含在@#%中间，例如：
```
@#% <咒语1> @#%
@#% <咒语2> @#%
```
咒语执行后，返回结果以#S#开头，#E#结尾，每条结果以[N]标识对应的咒语顺序。咒语仅在你与系统之间传递，用户不可见。